// Cargo expects source files to live in src directory

fn main() {
    println!("Hello, world!");
}

/* 
Key commands are:
    cargo build (builds, should be used for development)
    cargo run (builds if necessary, and runs executable)
    cargo check (makes sure that code compiles, but no executable produced)
    cargo build --release (optimized build, should be used for production)
*/
